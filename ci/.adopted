prompt_version: 11444ada79d08ada7698830d7e732c5c173b4d3df4a0fe6fc1bbae00b6b0981c
skeleton_sha:   c948ea022afaaa9a6fd217eaa53a78f980851cc5
adopted_date:   2026-08-30
steps_degraded: 15

# Step 15's current prompt text treats separate top-level reusable-workflow
# runs as duplicate jobs within one orchestrator run. GitHub records called
# workflows as separate top-level runs while each called job still executes
# once; the module's single pull_request entry point and cadence policy prove
# that safe topology. Revisit when the upstream prompt corrects its run model.
